<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.getfood">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <uses-feature android:name="android.hardware.fingerprint"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="io.getfood.modules.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="io.getfood.modules.home.HomeActivity" />
        <activity android:name="io.getfood.modules.home.ShoppingListActivity" />
        <activity android:name="io.getfood.modules.auth.LoginActivity" />
        <activity android:name="io.getfood.modules.getting_started.GettingStartedActivity" />
        <activity android:name="io.getfood.modules.getting_started.QRScanActivity" />
        <activity android:name="io.getfood.modules.auth.SignUpActivity" />
        <activity android:name="io.getfood.modules.auth.ProfileActivity" />
        <activity android:name="io.getfood.modules.family.FamilyActivity" />
    </application>

</manifest>